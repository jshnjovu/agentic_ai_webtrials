{
  "name": "leadgen-makeover-agent-backend",
  "version": "1.0.0",
  "description": "Backend API for LeadGen Makeover Agent with Google PageSpeed API integration",
  "scripts": {
    "test": "python -m pytest",
    "start": "python main.py",
    "dev": "uvicorn src.main:app --reload"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
