# Test dependencies for fast execution
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-xdist>=3.0.0
pytest-timeout>=2.1.0
pytest-mock>=3.10.0

# Core dependencies (should already be installed)
requests>=2.28.0
tenacity>=8.2.0
python-dotenv>=0.19.0

# Optional: for better test output
pytest-html>=3.1.0
pytest-json-report>=1.5.0
